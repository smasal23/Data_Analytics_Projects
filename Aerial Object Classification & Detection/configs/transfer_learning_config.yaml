experiment:
  name: "transfer_learning_baseline"

training:
  image_size: [224, 224]
  batch_size: 32
  epochs: 10
  learning_rate: 0.0001
  seed: 42

model:
  backbone: "resnet50"
  pretrained: true
  num_classes: 2
  freeze_backbone: true